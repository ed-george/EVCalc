package org.dynamium.etcalc.core

object CalculateMileage {
    fun calculateEUC(riderWeight: Int, airTemperature: Int, chargeCycles: Int, batterySize: Int, speedType: String): Int {
        val result: Int = 0
        return result
    }
}